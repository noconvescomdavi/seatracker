# NV2 Coordinate Research

Status: `UNKNOWN`.

For each coordinate hypothesis record: fixture, byte offset/range, scalar type, width, signedness, endianness, scale factor, projection/datum hypothesis, decoded value, expected geographic bounds and validation result.

Never infer coordinates solely because decoded numeric values look like latitude/longitude.
